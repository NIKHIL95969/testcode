export interface ProjectCardProps {
    title: string;
    description: string;
    liveLink: string;
    githubLink: string;
  }
  
  export interface ExperienceCardProps {
    companyName: string;
    duration: string;
    description: string;
  }
  
  export interface NavItemProps {
    label: string;
  }